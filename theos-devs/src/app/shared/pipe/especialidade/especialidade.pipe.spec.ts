import { EspecialidadePipe } from './especialidade.pipe';

describe('EspecialidadePipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadePipe();
    expect(pipe).toBeTruthy();
  });
});
